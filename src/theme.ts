interface IColors {
    black: string;
    white: string;
    grey: string;

}

const colors: IColors = {
    black: "#3F3F3F",
    white: "#FBFAF8",
    grey: "#AAAAAA"

}

const theme = {
    colors
}

export default theme;